# Proper Setup:
1. Must include scripts inside `/home/oracle/auto-dbhc-collector/` in each server

# Test Environment Caveats:
1. Mock Binaries (sqlplus, crsctl, lsnrctl)

# Question:
1. Do all instances share the same oracle home?
2. 